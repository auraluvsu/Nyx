# Treat all text files as UTF-8 and normalize line endings to LF (Unix style)
* text=auto eol=lf

# Go files: Just text, no fancy diffing needed
*.go text diff=go

# Python files: Pretty similar, but let's also make sure they stay textified
*.py text diff=python

# Shell scripts (you might have these for tooling)
*.sh text eol=lf

# Makefiles: LF line endings because make is picky
Makefile text eol=lf

# Ignore OS-specific files that shouldn't cause diff noise
*.DS_Store binary
Thumbs.db binary

# Binary files (images, compiled stuff etc.) - no diffing
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.exe binary
*.dll binary
*.so binary
*.zip binary
*.tar.gz binary
*.tgz binary

# Lock files — important to treat as text so Git doesn't mangle them
go.mod text
go.sum text
requirements.txt text

# Go files
*.go linguist-language=Go

# Python files
*.py linguist-language=Python

# Shell scripts
*.sh linguist-language=Shell

# Markdown docs
*.md linguist-language=Markdown

# HTML/CSS/JS (if you ever dabble in full-stack magic)
*.html linguist-language=HTML
*.css linguist-language=CSS
*.js linguist-language=JavaScript

# JSON & YAML configs
*.json linguist-language=JSON
*.yaml linguist-language=YAML
*.yml linguist-language=YAML

# SQL scripts
*.sql linguist-language=SQL
